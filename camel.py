# En algunos lenguajes de progrmación es muy importante utilizar la convensión  también conocida como "mixe case" para nombrar varialbles que consisten en varias palabras

cameL_case = input("ingrese el nombre de una variable en cameLcase")
snake_case = ""

for i in range (len(cameL_case)):
       if cameL_case .issuper():
        snake_case=+"_" + cameL_case[i]. lower()
else:
        snake_case=+ cameL_case[i]
        print("la variable en snake case es:", snake_case)
       